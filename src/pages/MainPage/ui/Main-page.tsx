import { RideList } from '@/entities/ride';
import { useTranslation } from 'react-i18next';

const MainPage = () => {
    return (
        <>
            {' '}
            <RideList />
        </>
    );
};

export default MainPage;
